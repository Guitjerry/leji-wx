<template name="not-my-fault">
  <view class="not-my-fault">
    <image wx:if="{{img}}" class="not-my-fault-img" src="{{img}}" mode="widthFix"></image>
    <view class="not-my-fault-msg">
      {{ msg }}
    </view>
  </view>
</template>
